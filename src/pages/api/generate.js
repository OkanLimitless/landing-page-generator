const { generateVSLPage, generateEcomPage } = require('@/lib/generators');
const { generatePrivacyPage, generateTermsPage } = require('@/lib/utils/legal-pages');
const { getRandomStyle } = require('@/lib/utils/style-variations');

const validateData = (data, type) => {...};

export default async function handler(req, res) {...}